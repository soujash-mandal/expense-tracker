const express = require("express");

const router = express.Router();

// Import and use individual routes
// router.use('/users', require('./userRoutes'));
// router.use('/products', require('./productRoutes'));

module.exports = router;
